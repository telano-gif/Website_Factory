# Website Factory

Reusable production system for taking a company from raw input to a finished website.

Pipeline: INTAKE → RESEARCH → DESIGN → CONTENT → BUILD → QA → DEPLOY

The factory consumes the separate `telano-gif/Design_Library` as read-only design intelligence and produces independent, deployable client websites for GitHub → Vercel.